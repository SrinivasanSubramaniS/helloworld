name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    uses: SrinivasanSubramaniS/helloworld/.github/workflows/github-actions-demo.yml@v1
 steps:
      - run: echo "🎉 The job was called in main-reusable.yml."
      
  call-workflow-passing-data:
    permissions:
      contents: read
      pull-requests: write
    uses: SrinivasanSubramaniS/helloworld/.github/workflows/workflow-B.yml@main
    with:
      config-path: .github/labeler.yml
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
